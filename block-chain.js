const blockChain = (data, prev) => {
  if (prev === null) {
    prev = { index: 0, hash: '0' };
  }
  return {
    index:
    hash: hashCode()
    data:
    prev:
    chain:
  }
};

// must return new object with those stats or null version
// index ?
// hash: has is func prov by them, needs index.string() prev.hash? and data stringify?
// data
// prev
// chain